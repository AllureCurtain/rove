name: RAG CI

on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "src/main.rs"
      - "src/lib.rs"
      - "src/interfaces/cli/index.rs"
      - "src/tools/mod.rs"
      - "src/tools/rag/**"
      - "src/bin/rove-index.rs"
      - "tests/cli_index.rs"
      - "tests/rag.rs"
      - ".github/workflows/rag-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "src/main.rs"
      - "src/lib.rs"
      - "src/interfaces/cli/index.rs"
      - "src/tools/mod.rs"
      - "src/tools/rag/**"
      - "src/bin/rove-index.rs"
      - "tests/cli_index.rs"
      - "tests/rag.rs"
      - ".github/workflows/rag-ci.yml"

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  CARGO_PROFILE_TEST_DEBUG: 0
  RUST_BACKTRACE: 1

jobs:
  rag-clippy:
    name: rag feature clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install native build tools
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-rag-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-rag-

      - name: cargo clippy --features rag
        run: cargo clippy --all-targets --features rag -- -D warnings

  rag-test:
    name: rag feature tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install native build tools
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-rag-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-rag-

      - name: cargo test --features rag
        run: cargo test --features rag

  rag-index-smoke:
    name: rag index smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install native build tools
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-rag-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-rag-

      - name: cargo check rove-index --features rag
        run: cargo check --features rag --bin rove-index

      - name: deterministic index smoke
        run: cargo test --features rag --test cli_index deterministic_index_run_writes_manifest -- --exact
