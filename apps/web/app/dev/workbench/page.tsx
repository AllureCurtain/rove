"use client";

import { RoveWorkbench } from "../../../components/rove-workbench";

/**
 * Temporary migration scaffold for the old developer workbench.
 * Not a primary product entry — default `/` is the new shell.
 */
export default function WorkbenchDevPage() {
  return <RoveWorkbench />;
}
