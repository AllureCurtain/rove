# rove-bench

Deterministic no-network benchmark schemas, checks, suite generation, and runner.

Local dependencies:

```text
rove-models
rove-runtime
rove-app-bootstrap
```

Focused verification:

```powershell
cargo test -p rove-bench
cargo run -p rove-bench -- --list
```
