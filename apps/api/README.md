# rove-api

HTTP job lifecycle, SSE, OpenAPI, auth/CORS/rate-limit, and benchmark endpoints.

Local dependencies:

```text
rove-models
rove-core
rove-runtime
rove-app-bootstrap
rove-bench
```

Focused verification:

```powershell
cargo test -p rove-api
cargo run -p rove-api
```
