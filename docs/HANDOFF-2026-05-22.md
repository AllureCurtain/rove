# Temporary Handoff - 2026-05-22

This is a temporary next-conversation handoff. It can be deleted after the next session has picked up the context.

## Current State

- Branch: `main`, tracking `origin/main`.
- Latest remote-tracking commit before this handoff work: `973bb27 feat: add api approval decisions`.
- User asked to stop new feature work, clean up the workspace, commit what should be committed, and leave a short handoff.
- No push was requested in the final instruction, so the next session should check whether the local commit still needs to be pushed.

## Recent Work

- Hardened early runtime milestones:
  - Added the hook pipeline with pre-tool and post-tool hooks.
  - Added hook blocking via `ToolError::HookBlocked`.
  - Routed engine tool execution through `Executor::with_hooks`.
  - Made workspace detection use the nearest git root and store state at `<repo>/.rove`.
  - Added StateStore APIs for exact `run_id` resume and session-filtered resumable task lists.
  - Added CLI resume resolution for `--resume latest` and `--resume <run_id>`.

- Continued planner/runtime cleanup:
  - Preserved summary memory on resume.
  - Added planner failure event handling and replanning after failed planned steps.
  - Added `plan_step_failed` handling in stream consumers.

- Continued approval/workbench work:
  - Added stdin approval provider for CLI ask mode.
  - Wired web workbench approval buttons to the API approval endpoint.
  - Added pending approval state in the web reducer and type definitions.
  - Added Vitest coverage for web client approval submission and workbench approval state.

- Updated docs:
  - `docs/00-README.md` and `docs/GOAL.md` now say the original milestone branch plan is historical.
  - `docs/HANDOFF-2026-05-21.md` was updated to reflect that the repo is already beyond M0-M5.

## Verification To Recheck

The last session should run and record fresh results before pushing:

```powershell
cargo fmt --all --check
cargo test --test e2e
cargo test --test api
cargo test interfaces::cli::oneshot::tests::resolve_resume_state
cd web-ui; npm test; npm run typecheck
```

If time is tight, the Rust e2e/API checks and web Vitest/typecheck are the highest-value checks for this batch.

## Next Steps

1. Check `git status --short --branch`.
2. If the local commit exists and verification is still good, push `main` to GitHub.
3. Continue the user's requested comparison between the current implementation and the original docs:
   - Start with `docs/04-架构与路线图.md`, `docs/05-下一步-统一执行内核.md`, and `docs/06-请求生命周期.md`.
   - Produce a table of implemented-as-designed, implemented-differently, and not-yet-implemented items.
4. Delete this temporary handoff after it is no longer needed.
